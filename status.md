# This is a reference implementation focused on deterministic behavior, inspectability, and clarity over breadth.
# It now includes a minimal local operator console, a local HTTP API, deterministic receipts, append-only JSONL audit logging,
# config-driven gates, operator escalation outcomes, and test coverage for the core evaluation and audit flow.
